# Public CSI data

The public CSI dataset is not committed to this repository because it is large.

For the experiments, the dataset was downloaded locally from the public CSI indoor-localization dataset repository and a 250-coordinate subset was symlinked under `data/raw/public_localization_250`.

The generated results are already included under:

- `reports/public_csi_location_250_fast/`
